'use client';
import ProgressBar from 'react-scroll-progress-bar';

const ScrollBar = () => {
    return (
        <ProgressBar height="6" bgcolor="#ff0000" duration="0.2" />
    );
}

export default ScrollBar;